# Logic name of the app
spring.application.name=service-A

#With Docker
#spring.cloud.config.uri=http://config:9999

# Go to config service, that goes to GIT to get configuration.
#Without Docker
spring.cloud.config.uri=http://localhost:9999

# Hardcoded port, but if there is another port in the cloud(GIT config(port 8001)), so it will override this port.
server.port=7001

#To try this later...
#spring.cloud.consul.discovery.management-port = 8500
